# Execution Log

## 2026-03-13

- Identified the primary SDK golden path as `AccountExplorer` -> `/enrich/queue` -> `/enrich/status` -> `executeEnrichmentStage()` -> `onEnrichmentComplete()` -> Sanity account reread.
- Reproduced a real failure on `/enrich/queue` for account `account-ca9b477c3c25726c` and confirmed a `500` caused by Sanity document quota exhaustion.
- Audited dataset counts and found `usageLog` and `userPattern` documents dominating storage.
- Disabled request-side telemetry writes by default in `src/services/action-logger.js` and `src/index.js` unless `ENABLE_REQUEST_LOGGING=1`.
- Fixed the SDK account-detail enrichment payload to send `accountId` and the real `canonicalUrl` instead of `https://<accountKey>`.
- Changed SDK polling to use `/enrich/status?...&advance=1` so the visible UI loop can drive stage-by-stage progress without waiting for cron.
- Changed worker-rendered account page polling to use the same advancing status path.
- Updated `handleGetEnrichmentStatus()` to optionally execute the next real enrichment stage before returning status.
- Fixed `enrichmentJob` contract drift so status polling can fall back to `_id` when `jobId` is missing.
- Fixed scan fetch headers to request `identity` encoding so scan HTML is decoded as text instead of compressed binary garbage.
- Mirrored completed pipeline stage outputs into top-level `accountPack.payload` fields so completeness scoring and downstream enrichers stop re-queuing finished work.
- Ran a real full pipeline for `Vercel`, confirmed `accountPack.payload.researchSet` persisted, and repaired the account pack/account fields into the canonical shape.
- Deleted a large backlog of `usageLog`, `userPattern`, `userInteraction`, and many old job documents to recover Sanity capacity and reduce type-drift pressure.
- Re-tested a fresh untouched account after the document-limit increase and confirmed the active failure is no longer document count, but Sanity type attribute pressure on new enrichment job creation.
- Added a queue fallback that returns a virtual job id (`virtual.<accountKey>`) when `/enrich/queue` hits the Sanity job-type ceiling instead of failing the UI trigger.
- Changed active-job detection to stop treating legacy `enrichmentJob` documents as runnable jobs, so stale records no longer hijack new status polling.
- Verified the fresh-account HTTP path now returns `200` from `/enrich/queue` and `/enrich/status` reports the virtual runtime path instead of the stale legacy job path.
- Confirmed the next remaining break is virtual-path persistence into `accountPack`, which still triggers Sanity's attribute ceiling for richer downstream stage payloads and needs a stricter final stored shape.
- Re-ran review on the hosted SDK app fetch failure and found the production deploy scripts were sourcing local `apps/sanity-data-sdk/.env`, which could bundle `VITE_WORKER_URL=http://localhost:8787` into the hosted app.
- Fixed `apps/sanity-data-sdk/package.json` so only `npm run dev` reads local `.env`; build/check/deploy now use production defaults instead of local overrides.
- Simplified `apps/sanity-data-sdk/src/lib/worker-api.ts` so read-only worker GETs use query-param auth and fewer headers, reducing cross-origin preflight pressure and surfacing clearer network errors.
- Extended `src/utils/molt-auth.js` to accept `apiKey` query auth so hosted GET snapshot/status requests can authenticate without custom headers.
- Improved `apps/sanity-data-sdk/src/components/EnrichmentView.tsx` queue error handling so network/worker failures render in the UI instead of silently resetting.
- Deployed the worker and redeployed the SDK app after the hosted fetch-path fixes.
- Mapped the Cloudflare worker into three execution roles: synchronous API/control plane in `fetch()`, queue-based OSINT execution in `queue()`, and cron fan-out in `scheduled()`.
- Optimized `src/index.js` so prompt parsing, request cloning, response-body capture, and autonomous-enrichment context setup are no longer paid on every request by default.
- Gated autonomous enrichment behind `ENABLE_AUTONOMOUS_ENRICHMENT=1` plus a prompt-bearing route allowlist, reducing unnecessary imports and post-response work on general API traffic.
- Fixed the Wrangler observability config to use valid top-level TOML syntax: `observability = { enabled = false }`.
- Split enrichment execution from status reads by making `GET /enrich/status` read-only and moving explicit advancement to `POST /enrich/advance`.
- Updated both the SDK app and worker-rendered account page so they call the explicit advance endpoint instead of using status polling as a mutating execution path.
- Added a lightweight `surface=sdk` snapshot path in `src/routes/operatorConsole.ts` so the SDK dashboard no longer pays for the full operator-console control-plane snapshot on every refresh.
- Reduced repeated Sanity reads in `src/routes/dq.ts` by caching accounts for the whole enrich run instead of re-fetching the full account collection inside each job iteration.
- Standardized the remaining `accountPack` write in `src/services/account-orchestrator.js` to the canonical dash ID format: `accountPack-${accountKey}`.
- Sanitized final virtual-path research-set writes in `src/services/enrichment-service.js`, including compact scan/linkedin shapes, and changed virtual completion writes to overwrite stage payloads instead of only appending `researchSet`.
- Added KV-backed virtual enrichment progress checkpoints so explicit `POST /enrich/advance` calls do not need to mutate Sanity just to remember in-flight stage state.
- Changed `handleAdvanceEnrichment()` to return the post-execution job state directly instead of re-reading status immediately and hiding advance errors behind stale status reads.
- Disabled `handleBrief()` auto-save when the brief stage is invoked from the enrichment pipeline so the virtual path avoids an extra mid-pipeline `accountPack` mutation.
