// Auto-generated by scripts/inline-openapi.js - do not edit
export default `openapi: 3.1.0
info:
  title: Website Scanner API
  version: 1.1.0
  description: |
    A free-deployable website scanner + research system that provides tech stack detection,
    website analysis, discovery/crawl, evidence extraction, web search, verification,
    and persistence into Sanity for account-level intelligence (company + URL).

servers:
  - url: https://website-scanner.austin-gilbert.workers.dev
    description: Production deployment URL

tags:
  - name: Health
  - name: Scanner
  - name: LinkedIn
  - name: Extraction
  - name: Search
  - name: Discovery
  - name: Crawling
  - name: Research
  - name: Verification
  - name: Sanity
  - name: Enrichment
  - name: Competitors
  - name: OSINT
  - name: Person Intelligence
  - name: Wrangler
  - name: Extension
  - name: MoltBot
  - name: Analytics
  - name: Network
  - name: SDR
  - name: Calls
  - name: UserPatterns
  - name: Webhooks

components:
  securitySchemes:
    MoltApiKey:
      type: apiKey
      in: header
      name: Authorization
      description: "Bearer token for Molt/ChatGPT API. In Custom GPT, use Authentication > API Key > Bearer and paste your MOLT_API_KEY."
  schemas:
    WranglerIngestRequest:
      type: object
      required: [userPrompt, gptResponse]
      properties:
        userPrompt:
          type: string
          description: User prompt sent to Wrangler
        gptResponse:
          type: string
          description: Wrangler response text
        sessionId:
          type: string
          nullable: true
        referencedAccounts:
          type: array
          items:
            type: string
        referencedPeople:
          type: array
          items:
            type: string
        contextTags:
          type: array
          items:
            type: string

    WranglerIngestResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [interactionId, sessionId, eventId, jobsQueued]
          properties:
            interactionId:
              type: string
            sessionId:
              type: string
            eventId:
              type: string
            jobsQueued:
              type: array
              items:
                type: string
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    MoltRunRequest:
      type: object
      required: [requestText]
      properties:
        requestText:
          type: string
          description: Natural language task for MoltBot
        mode:
          type: string
          enum: [auto, research, draft, plan, reflect]
          default: auto
        entityHints:
          type: array
          items:
            type: string
        requireApproval:
          type: boolean
          default: false

    MoltApproveRequest:
      type: object
      required: [approvalId, decision]
      properties:
        approvalId:
          type: string
        decision:
          type: string
          enum: [approve, reject]

    MoltLogRequest:
      type: object
      required: [text]
      properties:
        text:
          type: string
        channel:
          type: string
          default: manual
        outcome:
          type: string
          nullable: true
        entityHints:
          type: array
          items:
            type: string
    RequestId:
      type: string
      description: Request correlation ID
      example: abc123

    ErrorResponse:
      type: object
      required: [ok, error]
      properties:
        ok:
          type: boolean
          enum: [false]
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              example: BAD_REQUEST
            message:
              type: string
              example: Invalid request
            details:
              nullable: true
              description: Optional structured error details
              properties: {}
              additionalProperties: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    HealthResponse:
      type: object
      required: [ok]
      properties:
        ok:
          type: boolean
          enum: [true]

    SanityStatusResponse:
      type: object
      description: Sanity CMS connection status (single source of truth via sanity-client)
      properties:
        configured:
          type: boolean
          description: Whether SANITY_PROJECT_ID and token are set
        reachable:
          type: boolean
          nullable: true
          description: Whether a GROQ test query succeeded (null if not configured)
        projectId:
          type: string
          nullable: true
          description: Masked project id (***) when set
        dataset:
          type: string
          nullable: true
          description: Dataset name (default production)
        requestId:
          type: string
        ts:
          type: string
          format: date-time

    SitemapCheck:
      type: object
      required: [url, ok]
      properties:
        url:
          type: string
          format: uri
        ok:
          type: boolean
        status:
          type: integer
          nullable: true
        contentType:
          type: string
          nullable: true
        snippet:
          type: string
          nullable: true

    ScanResponse:
      type: object
      required:
        - input
        - finalUrl
        - status
        - headers
        - htmlSnippet
        - scriptSrcs
        - linkHrefs
        - robotsPresent
        - sitemapChecks
        - fetchedAt
      properties:
        input:
          type: string
          format: uri
          description: Original input URL
        finalUrl:
          type: string
          format: uri
          description: Final resolved URL after redirects
        status:
          type: integer
          description: HTTP status code
        headers:
          type: object
          description: Filtered HTTP headers (security-safe subset)
          properties: {}
          additionalProperties:
            type: string
        generator:
          type: string
          nullable: true
          description: Value from meta generator tag if present
        htmlSnippet:
          type: string
          description: First ~20KB of HTML content
        scriptSrcs:
          type: array
          items:
            type: string
        linkHrefs:
          type: array
          items:
            type: string
        robotsUrl:
          type: string
          format: uri
          nullable: true
        robotsPresent:
          type: boolean
        sitemapChecks:
          type: array
          items:
            \\\$ref: "#/components/schemas/SitemapCheck"
        technologyStack:
          type: object
          description: Detected CMS/frameworks/legacy/PIM/DAM/LMS and ROI/migration signals
          properties: {}
          additionalProperties: true
        businessUnits:
          type: object
          description: Detected business units / subdomains / org structure signals
          properties: {}
          additionalProperties: true
        digitalGoals:
          type: object
          description: Detected initiatives / transformation signals
          properties: {}
          additionalProperties: true
        jobAnalysis:
          type: object
          description: Careers page + role signals
          properties: {}
          additionalProperties: true
        aiReadiness:
          type: object
          description: AI readiness score + explanation
          properties: {}
          additionalProperties: true
        businessScale:
          type: object
          description: Traffic/revenue/cost indicators
          properties: {}
          additionalProperties: true
        performance:
          type: object
          description: Performance indicators and recommendations
          properties: {}
          additionalProperties: true
        strategy:
          type: string
          description: Scan strategy used when size limits apply
          enum: [standard, distributed, light]
          default: standard
        sizeLimitExceeded:
          type: boolean
          description: Indicates homepage exceeded size limit and fallback was used
        distributedCrawl:
          type: object
          nullable: true
          description: Results from distributed crawl fallback when homepage is too large
          properties: {}
          additionalProperties: true
        note:
          type: string
          nullable: true
          description: Human-readable note about fallback behavior
        fetchedAt:
          type: string
          format: date-time

    BatchScanResponse:
      type: object
      required: [summary, results, failed, scannedAt]
      properties:
        summary:
          type: object
          properties: {}
          additionalProperties: true
        results:
          type: array
          items:
            type: object
            properties: {}
            additionalProperties: true
        failed:
          type: array
          items:
            type: object
            properties: {}
            additionalProperties: true
        scannedAt:
          type: string
          format: date-time

    LinkedInProfileRequest:
      type: object
      required: [profileUrl]
      properties:
        profileUrl:
          type: string
          format: uri
          description: LinkedIn profile URL
        profileHtml:
          type: string
          description: Optional HTML/DOM export of the LinkedIn profile (manual fallback)
        profileText:
          type: string
          description: Optional pasted profile text (headline, experience, summary)

    GenericOkDataResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          properties: {}
          additionalProperties: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    ExtractRequest:
      type: object
      required: [url]
      properties:
        url:
          type: string
          format: uri
        mode:
          type: string
          enum: [fast, deep]
          default: fast
        maxChars:
          type: integer
          nullable: true
          default: 50000
        auto_enrich:
          type: boolean
          default: true
          description: Trigger autonomous enrichment in background
        refresh_if_stale:
          type: boolean
          default: true
          description: Re-enrich if existing data is older than 90 days
        verify_claims:
          type: boolean
          default: true
          description: Verify claims during enrichment when possible
        auto_advance:
          type: boolean
          default: true
          description: Auto-advance enrichment pipeline stages in background

    SearchRequest:
      type: object
      required: [query]
      properties:
        query:
          type: string
        limit:
          type: integer
          default: 10
          minimum: 1
          maximum: 50
        recencyDays:
          type: integer
          default: 30
          minimum: 1
        mode:
          type: string
          enum: [fast, deep]
          default: fast
        auto_enrich:
          type: boolean
          default: true
          description: Trigger autonomous enrichment in background
        refresh_if_stale:
          type: boolean
          default: true
          description: Re-enrich if existing data is older than 90 days
        verify_claims:
          type: boolean
          default: true
          description: Verify claims during enrichment when possible
        auto_advance:
          type: boolean
          default: true
          description: Auto-advance enrichment pipeline stages in background

    DiscoverRequest:
      type: object
      required: [url]
      properties:
        url:
          type: string
          format: uri
        budget:
          type: integer
          default: 20
          minimum: 1
          maximum: 50

    CrawlRequest:
      type: object
      required: [url]
      properties:
        url:
          type: string
          format: uri
        depth:
          type: integer
          enum: [1, 2]
          default: 1
        budget:
          type: integer
          default: 20
          minimum: 1
          maximum: 50
        includeTypes:
          type: array
          items:
            type: string

    BriefRequest:
      type: object
      required: [companyOrSite]
      properties:
        companyOrSite:
          type: string
        seedUrl:
          type: string
          format: uri
          nullable: true
        query:
          type: string
          nullable: true
        auto_enrich:
          type: boolean
          default: true
          description: Trigger autonomous enrichment in background
        refresh_if_stale:
          type: boolean
          default: true
          description: Re-enrich if existing data is older than 90 days
        verify_claims:
          type: boolean
          default: true
          description: Verify claims during enrichment when possible
        auto_advance:
          type: boolean
          default: true
          description: Auto-advance enrichment pipeline stages in background

    VerifyRequest:
      type: object
      required: [claims, sources]
      properties:
        claims:
          type: array
          items:
            type: string
        sources:
          type: array
          items:
            type: string
            format: uri

    CacheStatusResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [url, cached]
          properties:
            url:
              type: string
              format: uri
            cached:
              type: boolean
            cache:
              type: object
              nullable: true
              properties: {}
              additionalProperties: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    SchemaResponse:
      type: object
      required: [endpoints]
      properties:
        endpoints:
          type: object
          properties: {}
          additionalProperties: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    AccountIdentity:
      type: object
      required: [companyName, canonicalUrl]
      properties:
        companyName:
          type: string
          description: Company name / account name
        canonicalUrl:
          type: string
          format: uri
          description: Primary website URL for the account
        domain:
          type: string
          nullable: true
          description: Convenience field (e.g. example.com)
        aliases:
          type: array
          nullable: true
          items:
            type: string
          description: Alternate names / brands / known variations

    IngestionMetadata:
      type: object
      required: [source, ingestedAt]
      properties:
        source:
          type: string
          description: Where this payload came from (scanner, linkedin, manual, etc.)
          example: scanner
        ingestedAt:
          type: string
          format: date-time
        runId:
          type: string
          nullable: true
          description: Optional run identifier to tie multiple documents together
        tags:
          type: array
          nullable: true
          items:
            type: string

    AccountPack:
      type: object
      description: |
        A single normalized "account pack" capturing everything needed about an account.
        This is designed to be stored as a single Sanity document OR split into multiple docs.
      required: [account, collected, payload]
      properties:
        account:
          \\\$ref: "#/components/schemas/AccountIdentity"
        collected:
          \\\$ref: "#/components/schemas/IngestionMetadata"
        payload:
          type: object
          required: [scan]
          properties:
            scan:
              \\\$ref: "#/components/schemas/ScanResponse"
            batch:
              type: object
              nullable: true
              properties: {}
              additionalProperties: true
            discovery:
              type: object
              nullable: true
              properties: {}
              additionalProperties: true
            crawl:
              type: object
              nullable: true
              properties: {}
              additionalProperties: true
            evidence:
              type: object
              nullable: true
              properties: {}
              additionalProperties: true
            brief:
              type: object
              nullable: true
              properties: {}
              additionalProperties: true
            verification:
              type: object
              nullable: true
              properties: {}
              additionalProperties: true
            linkedin:
              type: object
              nullable: true
              properties: {}
              additionalProperties: true
            notes:
              type: object
              nullable: true
              properties: {}
              additionalProperties: true
          additionalProperties: false

    StoreType:
      type: string
      enum: [scan, linkedin, evidence, brief, account, person, interaction, session, learning]
      description: |
        Data type to store.
        - Use \\\`account\\\` to store a company/account with relationships (persons, tech stack).
        - Use \\\`interaction\\\` to store Q&A exchanges between user and GPT (WRANGLER).
        - Use \\\`session\\\` to group multiple interactions into one conversation.
        - Use \\\`learning\\\` to store derived insights from interactions.

    StoreRequest:
      type: object
      required: [account, data]
      properties:
        account:
          \\\$ref: "#/components/schemas/AccountIdentity"
        data:
          description: |
            Payload to store. For type=account, data can include:
            - companyName, canonicalUrl, domain (required via account object)
            - technologyStack (embedded object)
            - persons (array of person objects, will be stored separately and linked via references)
            - opportunityScore, aiReadiness, performance, businessScale, etc.
          type: object
          properties: {}
          additionalProperties: true
        options:
          type: object
          nullable: true
          description: Optional storage behavior flags
          properties:
            upsertBy:
              type: string
              nullable: true
              description: Field to upsert by (e.g. canonicalUrl, domain, externalId)
            dryRun:
              type: boolean
              nullable: true
              default: false
            merge:
              type: boolean
              nullable: true
              default: true
          additionalProperties: true

    StoreResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [stored, id, type]
          properties:
            stored:
              type: boolean
            id:
              type: string
              description: Sanity document ID
            type:
              \\\$ref: "#/components/schemas/StoreType"
            updated:
              type: boolean
              nullable: true
            canonicalUrl:
              type: string
              format: uri
              nullable: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    QueryType:
      type: string
      enum: [companies, search, context, quick]
      description: |
        Query type for GET /query endpoint.
        - \\\`companies\\\`: Query company accounts with filters (minScore, limit)
        - \\\`search\\\`: Search documents across types by term (requires \\\`q\\\` parameter)
        - \\\`context\\\`: Retrieve context and memory for intelligence system (interactions, learnings, follow-ups)
        - \\\`quick\\\`: Quick account lookup using \\\`queryType\\\` parameter (exists, profile, similar, search) - auto-triggers enrichment

    QueryGetResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          properties: {}
          additionalProperties: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    GroqQueryRequest:
      type: object
      required: [query]
      properties:
        query:
          type: string
          description: GROQ query string
        filters:
          type: object
          nullable: true
          properties: {}
          additionalProperties: true

    GroqQueryResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [documents, count]
          properties:
            documents:
              type: array
              items:
                type: object
                additionalProperties: true
            count:
              type: integer
            searchTerm:
              type: string
              nullable: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    UpdateRequest:
      type: object
      required: [patch]
      properties:
        patch:
          type: object
          description: Partial fields to update (Sanity patch semantics handled server-side)
          properties: {}
          additionalProperties: true
        options:
          type: object
          nullable: true
          properties: {}
          additionalProperties: true

    UpdateResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [updated, id]
          properties:
            updated:
              type: boolean
            id:
              type: string
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    DeleteResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [deleted, id]
          properties:
            deleted:
              type: boolean
            id:
              type: string
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    ResearchRequest:
      type: object
      required: [input]
      properties:
        input:
          type: string
          description: URL, company name, or account key
        inputType:
          type: string
          enum: [url, company, accountKey]
          default: url
        options:
          type: object
          properties: {}
          additionalProperties: true

    OneClickResearchRequest:
      type: object
      required: [input]
      properties:
        input:
          type: string
          description: URL, domain, company name, or account key
        inputType:
          type: string
          enum: [auto, url, domain, company, accountKey]
          default: auto
        includeCompetitors:
          type: boolean
          default: true
        includeComparison:
          type: boolean
          default: true
        mode:
          type: string
          enum: [fast, deep]
          default: fast
        autoEnrich:
          type: boolean
          default: true

    OrchestrateRequest:
      type: object
      required: [input]
      properties:
        input:
          type: string
          description: URL, company name, or account key
        inputType:
          type: string
          enum: [url, accountKey, company]
          default: url
        runMode:
          type: string
          enum: [queue, sync, async]
          default: queue
        options:
          type: object
          properties: {}
          additionalProperties: true

    ResearchResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          properties:
            orchestration:
              type: object
              properties: {}
              additionalProperties: true
            accountKey:
              type: string
            canonicalUrl:
              type: string
            companyName:
              type: string
            status:
              type: string
            stages:
              type: object
              properties: {}
              additionalProperties: true
            account:
              type: object
              properties: {}
              additionalProperties: true
            researchSet:
              type: object
              properties: {}
              additionalProperties: true
            competitorResearch:
              type: object
              properties: {}
              additionalProperties: true
            opportunities:
              type: array
              items:
                type: object
                properties: {}
                additionalProperties: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    IntelligenceResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          properties:
            intelligence:
              type: object
              properties: {}
              additionalProperties: true
            account:
              type: object
              properties: {}
              additionalProperties: true
            researchSet:
              type: object
              properties: {}
              additionalProperties: true
            competitorResearch:
              type: object
              properties: {}
              additionalProperties: true
            opportunities:
              type: array
              items:
                type: object
                properties: {}
                additionalProperties: true
            industryProfile:
              type: object
              properties: {}
              additionalProperties: true
            enrichmentStatus:
              type: object
              properties: {}
              additionalProperties: true
            completeness:
              type: object
              properties: {}
              additionalProperties: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    EnrichmentQueueRequest:
      type: object
      required: [canonicalUrl]
      properties:
        canonicalUrl:
          type: string
          format: uri
        accountKey:
          type: string
        options:
          type: object
          properties: {}
          additionalProperties: true

    EnrichmentQueueResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          properties:
            queued:
              type: boolean
            jobId:
              type: string
            status:
              type: string
            message:
              type: string
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    EnrichmentStatusResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          properties:
            status:
              type: object
              properties: {}
              additionalProperties: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    ResearchSetResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          properties:
            researchSet:
              type: object
              properties: {}
              additionalProperties: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    EnrichmentExecuteRequest:
      type: object
      required: [jobId]
      properties:
        jobId:
          type: string

    EnrichmentExecuteResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          properties:
            success:
              type: boolean
            job:
              type: object
              properties: {}
              additionalProperties: true
            completed:
              type: boolean
            progress:
              type: object
              properties: {}
              additionalProperties: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    EnrichmentJobsResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          properties:
            jobs:
              type: array
              items:
                type: object
                additionalProperties: true
            count:
              type: integer
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    CompetitorResearchRequest:
      type: object
      properties:
        accountKey:
          type: string
        canonicalUrl:
          type: string
          format: uri
        options:
          type: object
          properties: {}
          additionalProperties: true

    CompetitorResearchResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          properties:
            research:
              type: object
              properties: {}
              additionalProperties: true
            competitors:
              type: array
              items:
                type: object
                additionalProperties: true
            comparison:
              type: object
              properties: {}
              additionalProperties: true
            opportunities:
              type: array
              items:
                type: object
                additionalProperties: true
            insights:
              type: array
              items:
                type: object
                additionalProperties: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    OpportunitiesResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          properties:
            opportunities:
              type: array
              items:
                type: object
                additionalProperties: true
            industryProfile:
              type: object
              additionalProperties: true
            competitorCount:
              type: integer
            highPriorityCount:
              type: integer
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    OsintQueueRequest:
      type: object
      required: [url]
      properties:
        url:
          type: string
          format: uri
          description: Company website URL
        companyName:
          type: string
          nullable: true
          description: Optional company name
        mode:
          type: string
          enum: [year_ahead]
          default: year_ahead
          description: OSINT mode
        recencyDays:
          type: integer
          nullable: true
          default: 365
          description: How many days back to search
        force:
          type: boolean
          default: false
          description: Force re-run even if report exists

    OsintQueueResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [jobId, accountKey, queued]
          properties:
            jobId:
              type: string
              description: OSINT job ID
            accountKey:
              type: string
              description: Account key (hash of root domain)
            queued:
              type: boolean
            mode:
              type: string
              nullable: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    OsintStatusResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [status, stage, progress]
          properties:
            status:
              type: string
              enum: [queued, running, complete, failed]
            stage:
              type: integer
              description: Current pipeline stage (0-7)
            progress:
              type: integer
              description: Progress percentage (0-100)
            startedAt:
              type: string
              format: date-time
              nullable: true
            updatedAt:
              type: string
              format: date-time
              nullable: true
            completedAt:
              type: string
              format: date-time
              nullable: true
            error:
              type: string
              nullable: true
            reportId:
              type: string
              nullable: true
              description: Sanity document ID of report
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    OsintInitiative:
      type: object
      required: [title, importanceScore, confidence, timeHorizon, whyItMatters, evidence]
      properties:
        title:
          type: string
          description: Initiative title
        importanceScore:
          type: integer
          minimum: 0
          maximum: 100
          description: Importance score (0-100)
        confidence:
          type: string
          enum: [low, medium, high]
        timeHorizon:
          type: string
          enum: [0-3mo, 3-12mo, 12mo+]
        whyItMatters:
          type: string
          description: Explanation of why this matters
        evidence:
          type: array
          items:
            type: object
            required: [url]
            properties:
              url:
                type: string
                format: uri
              title:
                type: string
                nullable: true
              excerpt:
                type: string
                nullable: true
              publishedAt:
                type: string
                format: date-time
                nullable: true
              sourceType:
                type: string
                enum: [first_party, third_party, search_result]
                nullable: true
        status:
          type: string
          enum: [happening, being_decided, needing_execution, historical]
          nullable: true
          description: Initiative status based on timeline analysis
        progressPercent:
          type: integer
          minimum: 0
          maximum: 100
          nullable: true
          description: Progress percentage for active initiatives
        historicalReference:
          type: string
          nullable: true
          description: Reference to historical initiative if this is a continuation
        completionStatus:
          type: string
          enum: [completed, in_progress, delayed, cancelled]
          nullable: true
          description: Completion status for historical initiatives
        firstMentionedAt:
          type: string
          format: date-time
          nullable: true
          description: ISO timestamp when initiative was first mentioned
        expectedCompletionDate:
          type: string
          format: date-time
          nullable: true
          description: ISO timestamp for expected completion

    OsintReport:
      type: object
      required: [accountKey, canonicalUrl, rootDomain, dateRange, mode, generatedAt, executiveSummary, initiatives, risks, hiringSignals, digitalSignals, recommendedNextSteps, sources]
      properties:
        accountKey:
          type: string
        canonicalUrl:
          type: string
          format: uri
        rootDomain:
          type: string
        companyName:
          type: string
          nullable: true
        dateRange:
          type: object
          description: Rolling 12-month date range (start and end dates)
          properties:
            start:
              type: string
              format: date-time
            end:
              type: string
              format: date-time
        mode:
          type: string
        generatedAt:
          type: string
          format: date-time
        executiveSummary:
          type: array
          items:
            type: string
        initiatives:
          type: array
          items:
            \\\$ref: "#/components/schemas/OsintInitiative"
        historicalInitiatives:
          type: array
          description: Initiatives from 12 months ago for comparison
          items:
            \\\$ref: "#/components/schemas/OsintInitiative"
        timelineAnalysis:
          type: object
          description: Timeline analysis including completion rates and status changes
          properties: {}
          additionalProperties: true
        benchmarking:
          type: object
          description: Industry and competitor benchmarking data
          properties:
            industryBenchmark:
              type: object
              properties: {}
              additionalProperties: true
            competitorBenchmarks:
              type: array
              items:
                type: object
                properties: {}
                additionalProperties: true
            companyPosition:
              type: object
              properties: {}
              additionalProperties: true
            insights:
              type: array
              items:
                type: string
        risks:
          type: array
          items:
            type: string
        hiringSignals:
          type: array
          items:
            type: string
        digitalSignals:
          type: array
          items:
            type: string
        recommendedNextSteps:
          type: array
          items:
            type: string
        sources:
          type: array
          items:
            type: object
            required: [url]
            properties:
              url:
                type: string
                format: uri
              title:
                type: string
                nullable: true
              publishedAt:
                type: string
                format: date-time
                nullable: true
              score:
                type: integer
                nullable: true

    OsintReportResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [report]
          properties:
            report:
              \\\$ref: "#/components/schemas/OsintReport"
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    OsintRunRequest:
      type: object
      required: [url]
      properties:
        url:
          type: string
          format: uri
        companyName:
          type: string
          nullable: true
        mode:
          type: string
          enum: [year_ahead]
          default: year_ahead
        year:
          type: integer
          nullable: true
        recencyDays:
          type: integer
          nullable: true
        adminKey:
          type: string
          nullable: true
          description: Admin API key (required if ADMIN_API_KEY is set)

    OsintRunResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [success]
          properties:
            success:
              type: boolean
            reportId:
              type: string
              nullable: true
            report:
              \\\$ref: "#/components/schemas/OsintReport"
              nullable: true
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    PersonBriefRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          description: Person's name
          example: "John Doe"
        profileUrl:
          type: string
          format: uri
          description: LinkedIn profile URL or other profile URL
          example: "https://linkedin.com/in/johndoe"
        companyName:
          type: string
          description: Company name
          example: "Acme Corp"
        companyDomain:
          type: string
          description: Company domain (e.g., acme.com)
          example: "acme.com"
        mode:
          type: string
          enum: [fast, deep]
          default: fast
          description: Processing mode
        recencyDays:
          type: integer
          minimum: 1
          maximum: 365
          default: 365
          description: Number of days to look back for evidence
        crawlBudget:
          type: integer
          minimum: 1
          maximum: 30
          default: 20
          description: Maximum number of pages to crawl
        evidenceBudget:
          type: integer
          minimum: 1
          maximum: 10
          default: 6
          description: Maximum number of evidence sources to extract
        verify:
          type: boolean
          default: true
          description: Whether to verify claims with multiple sources
        store:
          type: boolean
          default: true
          description: Whether to store results in Sanity
        auto_enrich:
          type: boolean
          default: true
          description: Trigger autonomous enrichment in background
        refresh_if_stale:
          type: boolean
          default: true
          description: Re-enrich if existing data is older than 90 days
        verify_claims:
          type: boolean
          default: true
          description: Verify claims during enrichment when possible
        auto_advance:
          type: boolean
          default: true
          description: Auto-advance enrichment pipeline stages in background

    PersonBriefResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [personBrief]
          properties:
            personId:
              type: string
              description: Person document ID in Sanity
            accountKey:
              type: string
              description: Account key for the company
            briefId:
              type: string
              description: Brief document ID in Sanity
            evidenceIds:
              type: array
              items:
                type: string
              description: Array of evidence document IDs
            verificationId:
              type: string
              description: Verification document ID if verification was performed
            personBrief:
              type: object
              required: [executiveSummary, opportunityConfidence, evidenceInsights, personaLens, personLayer, execNameDrops, teamMapPreview, topRoiPlays, nextStepQuestion]
              additionalProperties: true
              properties:
                executiveSummary:
                  type: array
                  maxItems: 4
                  items:
                    type: string
                  description: Executive summary bullets (max 4, plain language)
                opportunityConfidence:
                  type: object
                  required: [score, confidence, marker, evidenceStrength, whatWouldChangeScore]
                  properties:
                    score:
                      type: number
                      minimum: 0
                      maximum: 100
                      description: Opportunity Confidence score (primary score)
                    confidence:
                      type: string
                      enum: [high, medium, low]
                      description: Confidence level
                    marker:
                      type: string
                      enum: [🟢, 🟡]
                      description: Confidence marker (🟢 = high, 🟡 = medium/low)
                    evidenceStrength:
                      type: string
                      enum: [strong, moderate, weak]
                      description: Strength of evidence supporting score
                    whatWouldChangeScore:
                      type: string
                      description: Explanation of what would change this score
                    supportingScores:
                      type: object
                      properties:
                        aiReadiness:
                          type: number
                          minimum: 0
                          maximum: 100
                        performance:
                          type: number
                          minimum: 0
                          maximum: 100
                        businessScale:
                          type: number
                          minimum: 0
                          maximum: 100
                      description: Supporting scores (secondary to Opportunity Confidence)
                evidenceInsights:
                  type: array
                  maxItems: 5
                  items:
                    type: object
                    required: [observed, interpreted, assumed]
                    properties:
                      observed:
                        type: string
                        maxLength: 300
                        description: What was observed (evidence)
                      interpreted:
                        type: string
                        maxLength: 300
                        description: What this means (insight)
                      assumed:
                        type: string
                        maxLength: 300
                        description: What we assume (if applicable)
                  description: Evidence → Insight → Assumption structure
                personaLens:
                  type: object
                  required: [primaryPersona, pain, gain, metric]
                  properties:
                    primaryPersona:
                      type: string
                      enum: [Engineering, Marketing, Digital, Product, IT, Security]
                      description: Single primary persona for framing
                    pain:
                      type: string
                      maxLength: 200
                      description: One pain point for this persona
                    gain:
                      type: string
                      maxLength: 200
                      description: One gain/benefit for this persona
                    metric:
                      type: string
                      description: One metric this persona cares about
                  description: Persona lens with one pain, one gain, one metric
                scores:
                  type: object
                  properties:
                    opportunity:
                      type: number
                      minimum: 0
                      maximum: 100
                    aiReadiness:
                      type: number
                      minimum: 0
                      maximum: 100
                    performance:
                      type: number
                      minimum: 0
                      maximum: 100
                    businessScale:
                      type: number
                      minimum: 0
                      maximum: 100
                  description: Legacy scores (use opportunityConfidence instead)
                personLayer:
                  type: object
                  required: [scope, decisionInfluence, dailyStatusQuo]
                  properties:
                    scope:
                      type: string
                      maxLength: 400
                      description: Inferred scope of responsibility
                    decisionInfluence:
                      type: string
                      maxLength: 400
                      description: Decision-making influence level
                    dailyStatusQuo:
                      type: string
                      maxLength: 400
                      description: Current daily priorities
                execNameDrops:
                  type: array
                  maxItems: 5
                  items:
                    type: object
                    required: [speaker, role, claim, url]
                    properties:
                      speaker:
                        type: string
                      role:
                        type: string
                      claim:
                        type: string
                        maxLength: 200
                      url:
                        type: string
                        format: uri
                      publishedAt:
                        type: string
                        format: date-time
                teamMapPreview:
                  type: object
                  required: [nodesCount, edgesCount]
                  properties:
                    nodesCount:
                      type: integer
                      minimum: 0
                    edgesCount:
                      type: integer
                      minimum: 0
                topRoiPlays:
                  type: array
                  maxItems: 3
                  items:
                    type: object
                    required: [what, why, who, personaLens]
                    properties:
                      what:
                        type: string
                        maxLength: 100
                      why:
                        type: string
                        maxLength: 200
                      who:
                        type: string
                        description: Single specific persona (not generic/multiple)
                      personaLens:
                        type: object
                        required: [primaryPersona, pain, gain, metric]
                        properties:
                          primaryPersona:
                            type: string
                            enum: [Engineering, Marketing, Digital, Product, IT, Security]
                          pain:
                            type: string
                            maxLength: 150
                          gain:
                            type: string
                            maxLength: 150
                          metric:
                            type: string
                  description: Top 3 ROI plays with persona lens (one pain, one gain, one metric)
                nextStepQuestion:
                  type: string
                  maxLength: 200
                  description: Conversational next-step question (advances conversation, not pitch)
          additionalProperties: false
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    GoodMorningRequest:
      type: object
      properties:
        daysBack:
          type: integer
          default: 30
          minimum: 1
          maximum: 365
          description: Number of days to look back for account pool
        minCallScore:
          type: integer
          default: 6
          minimum: 0
          maximum: 12
          description: Minimum priority score for calls (0-12)
        maxCalls:
          type: integer
          default: 25
          minimum: 1
          maximum: 50
          description: Maximum number of calls in queue
        maxLinkedIn:
          type: integer
          default: 15
          minimum: 1
          maximum: 30
          description: Maximum LinkedIn actions in queue
        maxEmails:
          type: integer
          default: 10
          minimum: 1
          maximum: 20
          description: Maximum emails in queue
        assumeRefresh:
          type: boolean
          default: false
          description: Force assumption refresh check
        log:
          type: boolean
          default: true
          description: Write to daily log file

    GoodMorningResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [date, winCondition, top10Accounts, callList, linkedInQueue, emailQueue, schedule, stats]
          properties:
            date:
              type: string
              format: date
              description: Date of routing plan
            winCondition:
              type: string
              description: Win condition for the day
            top10Accounts:
              type: array
              maxItems: 10
              items:
                type: object
                required: [account, accountKey, score, whyNow, bestNextAction]
                properties:
                  account:
                    type: string
                  accountKey:
                    type: string
                  canonicalUrl:
                    type: string
                    format: uri
                  score:
                    type: integer
                    minimum: 0
                    maximum: 12
                  whyNow:
                    type: string
                  bestNextAction:
                    type: string
                  owner:
                    type: string
                  contact:
                    type: string
                    format: uri
            callList:
              type: array
              maxItems: 25
              items:
                type: object
                required: [person, account, score, whyNow, talkTrack, objectionGuess, cta]
                properties:
                  person:
                    type: string
                  account:
                    type: string
                  accountKey:
                    type: string
                  score:
                    type: integer
                  whyNow:
                    type: string
                  talkTrack:
                    type: string
                  objectionGuess:
                    type: string
                  cta:
                    type: string
                  phone:
                    type: string
                  linkedIn:
                    type: string
                    format: uri
            linkedInQueue:
              type: array
              maxItems: 15
              items:
                type: object
                required: [person, account, state, action, personalization]
                properties:
                  person:
                    type: string
                  account:
                    type: string
                  accountKey:
                    type: string
                  state:
                    type: string
                    enum: [Not Connected, Connected, Engaged Recently, Dormant]
                  action:
                    type: string
                  personalization:
                    type: string
                  linkedInUrl:
                    type: string
                    format: uri
            emailQueue:
              type: array
              maxItems: 10
              items:
                type: object
                required: [person, account, reason, intent, cta]
                properties:
                  person:
                    type: string
                  account:
                    type: string
                  accountKey:
                    type: string
                  reason:
                    type: string
                  intent:
                    type: string
                  cta:
                    type: string
                  email:
                    type: string
                    format: email
            schedule:
              type: object
              properties:
                block1_calls:
                  type: string
                block2_calls:
                  type: string
                linkedin_block:
                  type: string
                admin_block:
                  type: string
                email_block:
                  type: string
            assumptionRefresh:
              type: object
              properties:
                triggered:
                  type: boolean
                accounts:
                  type: array
                  items:
                    type: object
                notes:
                  type: string
            stats:
              type: object
              properties:
                totalAccounts:
                  type: integer
                qualifiedAccounts:
                  type: integer
                callsQueued:
                  type: integer
                linkedInQueued:
                  type: integer
                emailsQueued:
                  type: integer
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    UserPatternStoreRequest:
      type: object
      required: [action]
      properties:
        userId:
          type: string
          description: User identifier (anonymized)
        userSegment:
          type: string
          enum: [sdr, ae, manager, analyst, executive]
          description: User segment
        action:
          type: string
          description: Action performed (e.g., scan, research, brief, good-morning-routing)
        approach:
          type: string
          description: How the user approached the task
        context:
          type: object
          properties:
            accountKey:
              type: string
            accountDomain:
              type: string
            intent:
              type: string
            persona:
              type: string
        outcome:
          type: string
          enum: [success, partial, failure, unknown]
          default: unknown
        timeSpent:
          type: integer
          description: Time spent in milliseconds
        toolsUsed:
          type: array
          items:
            type: string
          description: Endpoints/tools used
        sequence:
          type: array
          items:
            type: string
          description: Action sequence followed
        thinking:
          type: string
          description: Inferred thinking pattern or approach
        metadata:
          type: object
          description: Additional metadata

    UserPatternStoreResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [stored, patternId]
          properties:
            stored:
              type: boolean
            patternId:
              type: string
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

    UserPatternsResponse:
      type: object
      required: [ok, data, requestId]
      properties:
        ok:
          type: boolean
          enum: [true]
        data:
          type: object
          required: [queryType, filters, patterns]
          properties:
            queryType:
              type: string
              enum: [patterns, thinking, approaches, tools, sequences]
            filters:
              type: object
              properties:
                action:
                  type: string
                userSegment:
                  type: string
                outcome:
                  type: string
                limit:
                  type: integer
            patterns:
              type: object
              description: Pattern data (structure varies by queryType)
        requestId:
          \\\$ref: "#/components/schemas/RequestId"

paths:
  /wrangler/ingest:
    post:
      operationId: wranglerIngest
      summary: Store Wrangler interaction
      description: Store a Wrangler Q&A exchange and create a Molt event + jobs. Requires MOLT_API_KEY (Bearer or X-API-Key).
      tags: [Wrangler]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/WranglerIngestRequest"
      responses:
        "200":
          description: Stored Wrangler interaction
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/WranglerIngestResponse"
        "401":
          description: Missing or invalid API key (set MOLT_API_KEY in worker and use Authentication in GPT)
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
  /extension/capture:
    post:
      operationId: extensionCapture
      summary: Capture page data from Chrome extension
      description: |
        Accepts DOM-extracted data from the Molt Capture Chrome extension.
        Resolves accounts and people, creates technology documents, stores a
        capture event, and triggers background enrichment pipelines.
        Requires MOLT_API_KEY (Bearer or X-API-Key).
      tags: [Extension]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [url, source]
              properties:
                url:
                  type: string
                  description: URL of the captured page
                title:
                  type: string
                  description: Page title
                source:
                  type: string
                  enum: [linkedin, salesforce, hubspot, outreach, commonroom, looker, gong, apollo, zoominfo, 6sense, website]
                  description: Detected source platform
                capturedAt:
                  type: string
                  format: date-time
                accounts:
                  type: array
                  items:
                    type: object
                    properties:
                      name: { type: string }
                      domain: { type: string }
                      url: { type: string }
                      website: { type: string }
                      industry: { type: string }
                      about: { type: string }
                      headquarters: { type: string }
                      employeeCount: { type: string }
                      revenue: { type: string }
                      source: { type: string }
                people:
                  type: array
                  items:
                    type: object
                    properties:
                      name: { type: string }
                      headline: { type: string }
                      title: { type: string }
                      currentCompany: { type: string }
                      email: { type: string }
                      linkedinUrl: { type: string }
                      location: { type: string }
                      about: { type: string }
                      experience: { type: array, items: { type: object } }
                      skills: { type: array, items: { type: string } }
                      source: { type: string }
                technologies:
                  type: array
                  items:
                    type: string
                  description: Detected technologies on the page
                signals:
                  type: array
                  items:
                    type: object
                  description: Detected signals (churn risk, expansion, etc.)
                metadata:
                  type: object
                  description: Page meta tags (og:title, description, etc.)
                rawText:
                  type: string
                  description: Page body text (truncated to 15KB)
      responses:
        "200":
          description: Data captured and enrichment triggered
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean, enum: [true] }
                  data:
                    type: object
                    properties:
                      source: { type: string }
                      url: { type: string }
                      accountsResolved: { type: integer }
                      peopleResolved: { type: integer }
                      technologiesLinked: { type: integer }
                      entitiesResolved: { type: integer }
                      jobsQueued: { type: integer }
                      eventId: { type: string }
                      backgroundEnrichment: { type: boolean }
                  requestId: { type: string }
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
  /molt/run:
    post:
      operationId: moltRun
      summary: Run MoltBot orchestration
      description: Create a MoltBot request and execute the orchestration plan.
      tags: [MoltBot]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/MoltRunRequest"
      responses:
        "200":
          description: MoltBot run result
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
  /molt/approve:
    post:
      operationId: moltApprove
      summary: Approve or reject MoltBot action
      description: Resolve a pending MoltBot approval.
      tags: [MoltBot]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/MoltApproveRequest"
      responses:
        "200":
          description: Approval decision processed
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
  /molt/log:
    post:
      operationId: moltLog
      summary: Log MoltBot event
      description: Create a Molt event and enqueue jobs from the log.
      tags: [MoltBot]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/MoltLogRequest"
      responses:
        "200":
          description: Molt event logged
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
  /molt/jobs/run:
    post:
      operationId: moltJobsRun
      summary: Run queued Molt jobs
      description: Execute queued Molt jobs now.
      tags: [MoltBot]
      security:
        - MoltApiKey: []
      responses:
        "200":
          description: Jobs processed
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
  /health:
    get:
      operationId: healthCheck
      summary: Health check endpoint
      description: Returns a simple health status check
      tags: [Health]
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/HealthResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /sanity/status:
    get:
      operationId: sanityStatus
      summary: Sanity connection status
      description: Returns whether Sanity CMS is configured and reachable (uses sanity-client.js). Use this to verify content lake connectivity without calling /health.
      tags: [Health, Sanity]
      responses:
        "200":
          description: Status payload (configured, reachable, dataset)
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/SanityStatusResponse"

  /scan:
    get:
      operationId: scanHomepage
      summary: Scan a website homepage
      description: Scan homepage for stack, signals, and basic analysis for account intelligence. If the homepage exceeds size limits, a distributed crawl runs automatically and the response includes distributedCrawl results.
      tags: [Scanner]
      parameters:
        - name: url
          in: query
          required: true
          description: The URL of the website to scan (http or https)
          schema:
            type: string
            format: uri
      responses:
        "200":
          description: Successful scan
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ScanResponse"
        "400":
          description: Bad request (invalid URL or blocked URL)
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error or fetch failure
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /scan-batch:
    get:
      operationId: scanBatchAccounts
      summary: Scan multiple websites and return stack-ranked results
      description: Scans multiple websites in parallel and returns stack-ranked results.
      tags: [Scanner]
      parameters:
        - name: urls
          in: query
          required: true
          description: Comma-separated URLs or JSON array of URLs to scan (max 20)
          schema:
            type: string
      responses:
        "200":
          description: Successful batch scan with stack-ranked results
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/BatchScanResponse"
        "400":
          description: Bad request (invalid URLs or too many URLs)
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /linkedin-profile:
    post:
      operationId: scanLinkedInProfile
      summary: Scan public LinkedIn profile
      description: Scans a public LinkedIn profile and extracts data. LinkedIn may block automated requests.
      tags: [LinkedIn]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/LinkedInProfileRequest"
      responses:
        "200":
          description: Successful profile scan
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request (invalid URL or missing parameter)
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "403":
          description: LinkedIn blocked the request (bot protection or authentication required)
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /extract:
    post:
      operationId: extractEvidence
      summary: Extract structured evidence pack from URL
      description: Extracts structured data from a URL including cleaned text, excerpts, entities, signals, claims, and metadata. Results are cached.
      tags: [Extraction]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/ExtractRequest"
      responses:
        "200":
          description: Successful extraction
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /search:
    post:
      operationId: searchWeb
      summary: Web search with ranking and deduplication
      description: Performs web search with intelligent ranking, deduplication, and intent classification.
      tags: [Search]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/SearchRequest"
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /discover:
    post:
      operationId: discoverPages
      summary: Discover likely pages on a website
      description: Finds likely pages such as pricing, security, docs, careers, about, blog, and case studies.
      tags: [Discovery]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/DiscoverRequest"
      responses:
        "200":
          description: Discovered pages
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /crawl:
    post:
      operationId: distributedCrawl
      summary: Distributed crawl for large sites
      description: Targets specific pages intelligently for large sites, handles size limits gracefully, returns partial results. Use for sites like Sanity.io that exceed standard crawl limits.
      tags: [Crawling]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [baseUrl]
              properties:
                baseUrl:
                  type: string
                  format: uri
                  description: Base URL to crawl
                targetPages:
                  type: array
                  items:
                    type: string
                  description: Specific pages to target (e.g., ["/docs", "/studio"])
                autoDiscover:
                  type: boolean
                  default: true
                  description: Auto-discover key pages if targetPages not provided
                maxPages:
                  type: integer
                  default: 5
                  minimum: 1
                  maximum: 10
                  description: Maximum pages to crawl
                pageSizeLimit:
                  type: integer
                  default: 204800
                  description: Size limit per page in bytes (default 200KB)
      responses:
        "200":
          description: Distributed crawl results
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /crawl/smart:
    post:
      operationId: smartCrawl
      summary: Smart crawl with OSINT fallback
      description: Attempts distributed crawl first, automatically suggests OSINT scan if size limits exceeded. Ideal for very large sites.
      tags: [Crawling]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [baseUrl]
              properties:
                baseUrl:
                  type: string
                  format: uri
                  description: Base URL to crawl
                autoDiscover:
                  type: boolean
                  default: true
                  description: Auto-discover key pages
                useOsintFallback:
                  type: boolean
                  default: true
                  description: Auto-suggest OSINT if crawl fails
                maxPages:
                  type: integer
                  default: 5
                  minimum: 1
                  maximum: 10
      responses:
        "200":
          description: Crawl results or OSINT suggestion
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /brief:
    post:
      operationId: generateBrief
      summary: Generate action-ready research brief
      description: Generates a comprehensive research brief with citations from crawled data.
      tags: [Research]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/BriefRequest"
      responses:
        "200":
          description: Generated brief
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /research/complete:
    post:
      operationId: researchComplete
      summary: One-click complete research
      description: Runs full account research, enrichment, competitor analysis, and comparison in one request.
      tags: [Research]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/OneClickResearchRequest"
      responses:
        "200":
          description: Complete research response
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /research/quick:
    get:
      operationId: researchQuick
      summary: Quick account lookup with auto-enrichment
      description: Fast lookup by domain, accountKey, or url with optional enrichment trigger.
      tags: [Research]
      parameters:
        - name: domain
          in: query
          required: false
          schema:
            type: string
        - name: accountKey
          in: query
          required: false
          schema:
            type: string
        - name: url
          in: query
          required: false
          schema:
            type: string
            format: uri
      responses:
        "200":
          description: Quick research response
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /research/intelligence:
    get:
      operationId: researchIntelligence
      summary: Get complete account intelligence
      description: Returns full intelligence pack for an account key.
      tags: [Research]
      parameters:
        - name: accountKey
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Intelligence response
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/IntelligenceResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /orchestrate:
    post:
      operationId: orchestrateAccount
      summary: Unified orchestration pipeline
      description: Orchestrate the full company intelligence pipeline with staged execution.
      tags: [Research]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/OrchestrateRequest"
      responses:
        "200":
          description: Orchestration created or executed
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Service configuration error (Sanity not configured)
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /orchestrate/status:
    get:
      operationId: orchestrateStatus
      summary: Orchestration status
      description: Get orchestration job status and stage progress.
      tags: [Research]
      parameters:
        - name: jobId
          in: query
          required: false
          schema:
            type: string
        - name: accountKey
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Orchestration status
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /person/brief:
    post:
      operationId: generatePersonBrief
      summary: Generate person intelligence brief
      description: |
        Orchestrates person intelligence pipeline: resolves company/domain, scans homepage, discovers/crawls pages, extracts executive claims, verifies with multiple sources, builds team map, synthesizes brief with ROI insights. Stores in Sanity if enabled.
      tags: [Person Intelligence]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/PersonBriefRequest"
      responses:
        "200":
          description: Person brief generated successfully
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/PersonBriefResponse"
        "400":
          description: Bad request (missing required fields, invalid parameters)
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Service configuration error (Sanity not configured)
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /verify:
    post:
      operationId: verifyClaims
      summary: Verify claims against multiple sources
      description: Verifies claims by checking them against multiple source URLs.
      tags: [Verification]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/VerifyRequest"
      responses:
        "200":
          description: Verification results
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/GenericOkDataResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /store/{type}:
    post:
      operationId: storeData
      summary: Store data in Sanity CMS
      description: |
        Stores scan results, LinkedIn profiles, evidence packs, research briefs, person data,
        or a consolidated account (company) with 1-to-many relationships to persons and tech stacks.
        Accounts are the primary entity; persons and tech stacks are linked via references.
      tags: [Sanity]
      parameters:
        - name: type
          in: path
          required: true
          schema:
            \\\$ref: "#/components/schemas/StoreType"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/StoreRequest"
      responses:
        "200":
          description: Data stored successfully
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/StoreResponse"
        "400":
          description: Bad request (invalid type or missing data)
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error or Sanity not configured
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /query:
    get:
      operationId: queryData
      summary: Query Sanity documents or retrieve context
      description: |
        Query by type: \\\`companies\\\` (minScore/limit), \\\`search\\\` (q required), or \\\`context\\\` (memory). For recall/summarize use type=context, contextType=all, fullInsights=true, contextLimit=20, accountKey/domain. Returns stored account intelligence; missing data auto-enriched on context query.
      tags: [Sanity]
      parameters:
        - name: type
          in: query
          required: false
          schema:
            \\\$ref: "#/components/schemas/QueryType"
          description: Query type (companies, search, or context)
        - name: q
          in: query
          required: false
          schema:
            type: string
          description: Search term (required for type=search)
        - name: minScore
          in: query
          required: false
          schema:
            type: integer
          description: Minimum opportunity score (for type=companies)
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 10
          description: Result limit
        - name: accountKey
          in: query
          required: false
          schema:
            type: string
          description: Account key for context retrieval (type=context)
        - name: domain
          in: query
          required: false
          schema:
            type: string
          description: Domain for context retrieval (type=context)
        - name: contextType
          in: query
          required: false
          schema:
            type: string
            enum: [summary, interactions, learnings, followUps, all]
            default: summary
          description: Context type for retrieval (type=context)
        - name: contextLimit
          in: query
          required: false
          schema:
            type: integer
            default: 10
          description: Limit for context results (type=context); use 20+ for full recall/summarize
        - name: fullInsights
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: If true, return full interaction/learning text (no truncation) for recall/summarize (type=context)
        - name: minRelevanceScore
          in: query
          required: false
          schema:
            type: number
            default: 0.7
          description: Minimum relevance score for learnings (type=context)
        - name: tags
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated context tags for filtering (type=context)
        - name: url
          in: query
          required: false
          schema:
            type: string
            format: uri
          description: URL for quick lookup (type=quick)
        - name: companyName
          in: query
          required: false
          schema:
            type: string
          description: Company name for quick lookup (type=quick)
        - name: queryType
          in: query
          required: false
          schema:
            type: string
            enum: [exists, profile, similar, search, account, pack]
          description: Quick query subtype (when type=quick) - account, pack, profile, similar, search, exists
        - name: key
          in: query
          required: false
          schema:
            type: string
          description: Account key for quick lookup (type=quick)
      responses:
        "200":
          description: Query results or context retrieved successfully
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/QueryGetResponse"
        "400":
          description: Bad request (invalid type or missing required parameters)
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error or Sanity not configured
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
  /update/{docId}:
    put:
      operationId: updateDocument
      summary: Update or delete a Sanity document
      description: Updates an existing document in Sanity by document ID. Use DELETE method to delete.
      tags: [Sanity]
      parameters:
        - name: docId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/UpdateRequest"
      responses:
        "200":
          description: Document updated successfully
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/UpdateResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error or Sanity not configured
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteDocument
      summary: Delete a Sanity document
      description: Deletes a document from Sanity by document ID. Use cascade=true to delete related docs by accountKey when supported.
      tags: [Sanity]
      parameters:
        - name: docId
          in: path
          required: true
          schema:
            type: string
        - name: cascade
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: If true, cascades deletion to related documents for the same accountKey.
      responses:
        "200":
          description: Document deleted successfully
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/DeleteResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error or Sanity not configured
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /osint/queue:
    post:
      operationId: queueOsintJob
      summary: Queue OSINT year-ahead intelligence job
      description: |
        Queues an asynchronous OSINT pipeline job to generate year-ahead company intelligence.
        The job will run through discovery, web search, extraction, and synthesis stages.
      tags: [OSINT]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              \\\$ref: "#/components/schemas/OsintQueueRequest"
      responses:
        "200":
          description: Job queued successfully
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/OsintQueueResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /osint/status:
    get:
      operationId: getOsintStatus
      summary: Get OSINT job status
      description: Returns the current status of an OSINT job by account key for the current rolling 12-month period.
      tags: [OSINT]
      parameters:
        - name: accountKey
          in: query
          required: true
          description: Account key (from /osint/queue response)
          schema:
            type: string
      responses:
        "200":
          description: Job status
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/OsintStatusResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Job not found
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  /osint/report:
    get:
      operationId: getOsintReport
      summary: Get OSINT report
      description: Retrieves the generated OSINT report for an account, including timeline tracking and industry benchmarking for the current rolling 12-month period.
      tags: [OSINT]
      parameters:
        - name: accountKey
          in: query
          required: true
          description: Account key
          schema:
            type: string
      responses:
        "200":
          description: OSINT report
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/OsintReportResponse"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Report not found
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"
        "500":
          description: Internal server error
          content:
            application/json:
              schema:
                \\\$ref: "#/components/schemas/ErrorResponse"

  # ═══════════════════════════════════════════════════════════════════════
  # Competitors
  # ═══════════════════════════════════════════════════════════════════════

  /competitors/research:
    post:
      operationId: competitorsResearch
      summary: Research competitors for an account
      description: |
        Discovers and researches competitors for the given company.
        Pulls data from Sanity account records and web research.
      tags: [Competitors]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [domain]
              properties:
                domain:
                  type: string
                  description: Root domain of the target company
                accountKey:
                  type: string
                limit:
                  type: integer
                  default: 5
      responses:
        "200":
          description: Competitor research results
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  /competitors/opportunities:
    post:
      operationId: competitorOpportunities
      summary: Identify competitive displacement opportunities
      description: |
        Analyzes competitor weaknesses and identifies displacement
        opportunities for the given account.
      tags: [Competitors]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [domain]
              properties:
                domain:
                  type: string
                accountKey:
                  type: string
      responses:
        "200":
          description: Competitive opportunities
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  # ═══════════════════════════════════════════════════════════════════════
  # Enrichment (additional routes)
  # ═══════════════════════════════════════════════════════════════════════

  /enrich/run:
    post:
      operationId: enrichRun
      summary: Run enrichment for an account
      description: |
        Triggers the full enrichment pipeline for a specific account.
        Runs gap-fill orchestration, Content OS enrichment, and completeness scoring.
      tags: [Enrichment]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                domain:
                  type: string
                accountKey:
                  type: string
                stages:
                  type: array
                  items:
                    type: string
                  description: "Optional specific stages to run: scan, discovery, crawl, extract, linkedin, brief, verify"
      responses:
        "200":
          description: Enrichment initiated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  /enrich/status:
    get:
      operationId: enrichStatus
      summary: Get enrichment status for an account
      description: Returns the current enrichment pipeline status, completeness score, and gap analysis.
      tags: [Enrichment]
      security:
        - MoltApiKey: []
      parameters:
        - name: accountKey
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Enrichment status
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  /enrich/queue:
    post:
      operationId: enrichQueue
      summary: Queue an enrichment job
      description: Queues an enrichment job for later processing by the cron scheduler.
      tags: [Enrichment]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [domain]
              properties:
                domain:
                  type: string
                priority:
                  type: string
                  enum: [low, medium, high]
                  default: medium
      responses:
        "200":
          description: Job queued
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  # ═══════════════════════════════════════════════════════════════════════
  # Analytics
  # ═══════════════════════════════════════════════════════════════════════

  /analytics/dashboard:
    get:
      operationId: analyticsDashboard
      summary: Get analytics dashboard
      description: |
        Returns aggregate analytics across all accounts including
        enrichment progress, technology distribution, industry breakdown,
        and opportunity pipeline.
      tags: [Analytics]
      security:
        - MoltApiKey: []
      responses:
        "200":
          description: Dashboard analytics
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  /analytics/compare:
    post:
      operationId: analyticsCompare
      summary: Compare accounts
      description: Side-by-side comparison of two or more accounts across key dimensions.
      tags: [Analytics]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [accountKeys]
              properties:
                accountKeys:
                  type: array
                  items:
                    type: string
                  minItems: 2
                dimensions:
                  type: array
                  items:
                    type: string
                  description: "Dimensions to compare: technology, size, industry, aiReadiness"
      responses:
        "200":
          description: Comparison results
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  /analytics/trends:
    post:
      operationId: analyticsTrends
      summary: Get trends across accounts
      description: Detects patterns and trends across the account dataset.
      tags: [Analytics]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                dimension:
                  type: string
                  description: "Dimension to analyze: technology, industry, aiReadiness, signals"
                timeRange:
                  type: string
                  description: "Time range: 7d, 30d, 90d, all"
                  default: "30d"
      responses:
        "200":
          description: Trend analysis
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  # ═══════════════════════════════════════════════════════════════════════
  # LinkedIn Search
  # ═══════════════════════════════════════════════════════════════════════

  /linkedin/search:
    post:
      operationId: linkedinSearch
      summary: Search LinkedIn for people
      description: |
        Searches LinkedIn for people matching the given criteria.
        Results are stored in Sanity as person documents.
      tags: [LinkedIn]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                query:
                  type: string
                  description: Search query (name, title, company, keywords)
                company:
                  type: string
                title:
                  type: string
                location:
                  type: string
                limit:
                  type: integer
                  default: 10
      responses:
        "200":
          description: LinkedIn search results
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  # ═══════════════════════════════════════════════════════════════════════
  # Network
  # ═══════════════════════════════════════════════════════════════════════

  /network/importConnections:
    post:
      operationId: networkImportConnections
      summary: Import LinkedIn connections
      description: |
        Import LinkedIn connections data into Sanity.
        Creates person documents and links them to accounts.
      tags: [Network]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [connections]
              properties:
                connections:
                  type: array
                  items:
                    type: object
                    properties:
                      name:
                        type: string
                      company:
                        type: string
                      title:
                        type: string
                      linkedinUrl:
                        type: string
      responses:
        "200":
          description: Import results
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  /network/dailyRun:
    post:
      operationId: networkDailyRun
      summary: Run daily network briefing
      description: Generates daily network intelligence briefing based on connection activity and signals.
      tags: [Network]
      security:
        - MoltApiKey: []
      responses:
        "200":
          description: Daily briefing generated
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  # ═══════════════════════════════════════════════════════════════════════
  # SDR / Accountability
  # ═══════════════════════════════════════════════════════════════════════

  /sdr/good-morning:
    post:
      operationId: sdrGoodMorning
      summary: SDR daily good-morning briefing
      description: |
        Generates a personalized daily briefing for SDRs including
        prioritized accounts, tasks, signals, and coaching tips.
      tags: [SDR]
      security:
        - MoltApiKey: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                userId:
                  type: string
                focus:
                  type: string
                  description: Optional focus area for the briefing
      responses:
        "200":
          description: Good morning briefing
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  # ═══════════════════════════════════════════════════════════════════════
  # Calls
  # ═══════════════════════════════════════════════════════════════════════

  /calls/ingest:
    post:
      operationId: callsIngest
      summary: Ingest call recording data
      description: |
        Ingests call recording transcripts and metadata.
        Extracts insights, generates coaching tips, and creates follow-up tasks.
      tags: [Calls]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [transcript]
              properties:
                transcript:
                  type: string
                  description: Full call transcript text
                participants:
                  type: array
                  items:
                    type: string
                accountKey:
                  type: string
                duration:
                  type: number
                  description: Call duration in seconds
                callDate:
                  type: string
                  format: date-time
      responses:
        "200":
          description: Call processed
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  # ═══════════════════════════════════════════════════════════════════════
  # User Patterns
  # ═══════════════════════════════════════════════════════════════════════

  /user-patterns/query:
    get:
      operationId: userPatternsQuery
      summary: Query learned user patterns
      description: Returns learned patterns about user behavior, preferences, and successful strategies.
      tags: [UserPatterns]
      security:
        - MoltApiKey: []
      parameters:
        - name: category
          in: query
          schema:
            type: string
          description: "Filter by pattern category: messaging, timing, persona, channel"
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: User patterns
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  /user-patterns/store:
    post:
      operationId: userPatternsStore
      summary: Store a user pattern
      description: Records a user behavior pattern for future learning and recommendations.
      tags: [UserPatterns]
      security:
        - MoltApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [category, pattern]
              properties:
                category:
                  type: string
                  description: "Pattern category: messaging, timing, persona, channel"
                pattern:
                  type: string
                  description: Description of the observed pattern
                context:
                  type: object
                  description: Additional context about when/where the pattern was observed
                confidence:
                  type: number
                  description: Confidence score 0-1
      responses:
        "200":
          description: Pattern stored
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object

  # ═══════════════════════════════════════════════════════════════════════
  # Webhooks
  # ═══════════════════════════════════════════════════════════════════════

  /webhooks/sanity:
    post:
      operationId: sanityWebhook
      summary: Sanity document webhook (auto-enrichment trigger)
      description: |
        Receives webhook notifications from Sanity when account documents are
        created or updated. Automatically triggers gap-fill enrichment if the
        account's profile completeness is below 80% or has identified gaps.

        Setup in Sanity: Manage → API → Webhooks → Create webhook pointing to
        this URL, filtered on \\\`_type == "account"\\\`, with projection
        \\\`{_id, accountKey, domain, rootDomain, profileCompleteness}\\\`.

        Optionally secured with HMAC-SHA256 via SANITY_WEBHOOK_SECRET.
      tags: [Webhooks]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                _id:
                  type: string
                _type:
                  type: string
                accountKey:
                  type: string
                domain:
                  type: string
                rootDomain:
                  type: string
                profileCompleteness:
                  type: object
                  properties:
                    score:
                      type: number
                    gaps:
                      type: array
                      items:
                        type: string
      responses:
        "200":
          description: Webhook processed
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                  data:
                    type: object`;
