# Copy to .env.local. Do not commit .env.local.

# Worker base URL (NEXT_PUBLIC_* is used by client; WORKER_BASE_URL by server)
NEXT_PUBLIC_API_URL=
WORKER_BASE_URL=

# Optional: worker auth (must match worker ADMIN_TOKEN / MOLT_API_KEY if required)
WORKER_ADMIN_TOKEN=
WORKER_API_KEY=
